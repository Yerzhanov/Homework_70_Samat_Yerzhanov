superuser: admin   password: root

# from todo.models import Todolist
#
# Todolist.objects.all()
# Todolist.objects.filter(date_completed__isnull=False)
# Todolist.objects.filter(status='NOT_ACTIVE')
# Todolist.objects.filter(status='ACTIVE')
# Todolist.objects.values('id', 'title', 'memo', 'status')
#
#
# from issue_tracker.models import
#
# Ticket.objects.filter(task_type='BUG')
# Ticket.objects.values('task_type').order_by('task_type')
# Ticket.objects.values('id', 'title', 'text', 'status')
